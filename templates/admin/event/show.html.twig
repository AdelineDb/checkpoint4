{% extends 'base.html.twig' %}

{% block title %}Event{% endblock %}

{% block body %}

    <div class="general row">

        {% include '/inc/navbarAdmin.html.twig' %}

        <div class="col">

            <h1>Evénement</h1>

            <table class="table">
                <tbody>
                <tr>
                    <th>Lieu</th>
                    <td>{{ event.city |capitalize }}</td>
                </tr>
                <tr>
                    <th>Pays</th>
                    <td>{{ event.country | capitalize }}</td>
                </tr>
                <tr>
                    <th>Date et heure</th>
                    <td>{{ event.eventdate ? event.eventdate|date('Y-m-d H:i') : '' }}</td>
                </tr>
                </tbody>
            </table>

            <div class="row">

                <a class="btn btn-primary" href="{{ path('admin_event_edit', {'id': event.id}) }}">Modifier</a>

                <a class="btn"> {{ include('admin/event/_delete_form.html.twig') }} </a>

            </div>
        </div>
    </div>
{% endblock %}
