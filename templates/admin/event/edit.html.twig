{% extends 'base.html.twig' %}

{% block title %}Edit Event{% endblock %}

{% block body %}

    <div class="general row">

        {% include '/inc/navbarAdmin.html.twig' %}

        <div class="col">

            <h1>Modification de l'évènement</h1>

            {{ include('admin/event/_form.html.twig', {'button_label': 'Modifier'}) }}

            <a class="btn btn-primary" href="{{ path('admin_event_index') }}">Retour à l'accueil</a>

            {{ include('admin/event/_delete_form.html.twig') }}

        </div>
    </div>

{% endblock %}
